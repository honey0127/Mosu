import 'package:flutter/material.dart';

class AppColors {
  static const primary           = Color(0xFF7DB879);
  static const primaryDark       = Color(0xFF5A9A4A);
  static const primaryLight      = Color(0xFFB5D4A8);
  static const primaryExtraLight = Color(0xFFE8F3E3);
  static const background        = Color(0xFFF2F2F0);
  static const white             = Color(0xFFFFFFFF);
  static const textPrimary       = Color(0xFF1A1A1A);
  static const textSecondary     = Color(0xFF888888);
  static const error             = Color(0xFFE57373);
  static const chipBorder        = Color(0xFFDDDDDD);
  // Legacy aliases kept for screens that haven't migrated
  static const primaryDim        = primaryDark;
  static const fitBg             = primaryExtraLight;
  static const dareBg            = Color(0xFFFEEDE8);
  static const dareAccent        = Color(0xFFD85A30);
  static const surface           = Color(0xFF1A1A24);
  static const border            = Color(0xFF2C2C3E);
  static const textSub           = Color(0xFF888899);
}
